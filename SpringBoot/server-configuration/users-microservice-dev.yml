spring:
  application:
    name: user-microservice
  cloud:
    config:
      enabled: true

server:
  port: 8084

eureka:
  instance:
    prefer-ip-address: true
    hostname: localhost
    instance-id: ${spring.application.name}
